<h1>ユーザ一覧</h1>
<% @users.each do |user| %>
  <li class="user_list">
    <%= link_to "#{user.last_name} #{user.first_name}さん", user_path(user.id) %>
  </li>
<% end %>

<ul>
  <li><%= link_to "トップページに戻る", root_path %></li>
<ul>